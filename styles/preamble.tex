\usepackage{multicol}
\usepackage{lettrine}     % For elegant decorative drop caps
\usepackage{tcolorbox}    % For subtle tribute callout boxes
\usepackage{enumitem}

% Configure two-column separator and spacing
\setlength{\columnsep}{16pt}
\setlength{\columnseprule}{0.4pt}
\renewcommand{\columnseprulecolor}{\color{rulecolor}} % Uses defined light gray rulecolor